apiVersion: v1
kind: Service
metadata:
  name: blog-frontend-service
  namespace: blog
  labels:
    app: blog-frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: blog-frontend